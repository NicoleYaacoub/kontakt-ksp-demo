on init
    make_perfview
    set_ui_height_px(70)

    declare ui_knob $pan (0, 100, 1)
    set_text($pan, "Pan")
    $pan := 50
end on

on ui_control($pan)
    set_engine_par($ENGINE_PAR_PAN, (($pan * 200) - 10000), -1, -1, -1)
end on